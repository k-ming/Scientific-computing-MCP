# 复制为 .env 并填入真实值（.env 不要提交到版本库）。
# 放在 docker-compose.yml 所在目录，compose 会自动读取。

# Kiro headless 模式的 API Key（在 https://app.kiro.dev/ 生成）。
KIRO_API_KEY=your-kiro-api-key

# 网关自身的访问令牌，安卓 app 用它做 Bearer 鉴权（自己随机生成一个长字符串）。
GATEWAY_TOKEN=change-me-to-a-long-random-string
