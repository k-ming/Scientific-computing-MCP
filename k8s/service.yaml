apiVersion: v1
kind: Service
metadata:
  name: scicalc-mcp
  labels:
    app: scicalc-mcp
spec:
  # 集群内部服务，由 Ingress 对外暴露。
  type: ClusterIP
  selector:
    app: scicalc-mcp
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
